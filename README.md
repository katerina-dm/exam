# 🐍 Итоговый проект: Агрегатор Цитат

Приветствую, коллега! Поздравляю с выходом на финишную прямую курса.
Сегодня твоя задача — доработать прототип веб-приложения. Старший разработчик подготовил для тебя структуру проекта, настроил окружение и написал базовый каркас, но **самую важную логику предстоит написать тебе**.

## 🎯 Суть проекта
Мы делаем приложение **"Цифровой цитатник"**.
Оно должно уметь:
1.  Заходить на сайт `quotes.toscrape.com`.
2.  Собирать оттуда цитаты и авторов (Парсинг).
3.  Сохранять их в нашу локальную базу данных SQLite.
4.  Красиво отображать сохраненные цитаты на главной странице сайта.

У тебя есть **3 часа**. Не торопись, читай комментарии в коде.

---

## 🛠 Установка и запуск

Прежде чем писать код, нужно подготовить рабочее место.

1.  **Открой папку проекта** в VS Code.
2.  **Создай виртуальное окружение**:
    *   Windows: `python -m venv venv`
    *   Mac/Linux: `python3 -m venv venv`
3.  **Активируй его**:
    *   Windows: `venv\Scripts\activate`
    *   Mac/Linux: `source venv/bin/activate`
4.  **Установи библиотеки**:
    ```bash
    pip install -r requirements.txt
    ```

---

## 📝 Твои задачи (Step-by-Step)

В файлах проекта оставлены комментарии с пометкой **`ЗАДАНИЕ`**. Иди строго по порядку:

### Шаг 1. База данных 🗄️
*   **Файл:** `database.py`
*   **Задача:** Описать модель таблицы `Quote`. Нам нужны поля `id`, `text` и `author`.

### Шаг 2. Парсер 🕷️
*   **Файл:** `parser.py`
*   **Задача:** Дописать функцию `get_quotes_from_web`. Она должна зайти на сайт, найти карточки с цитатами и вернуть список данных.

### Шаг 3. Логика приложения 🧠
*   **Файл:** `main.py`
*   **Задача:**
    *   В функции `home_page`: достать данные из БД и передать в шаблон.
    *   В функции `parse_data`: получить данные от парсера и сохранить их в БД.

### Шаг 4. Интерфейс (HTML) 🎨
*   **Файл:** `templates/index.html`
*   **Задача:**
    *   Наследовать шаблон от `base.html`.
    *   Написать цикл `for` (на языке Jinja), чтобы вывести все цитаты в виде карточек.

---

## 🆘 Если застрял (Папка `hints`)

В папке `hints/` лежат текстовые файлы с **готовыми решениями** для каждого этапа.

*   Попробуй написать сам.
*   Посмотри в свои конспекты или Google.
*   Если совсем тупик — открывай подсказку. Это нормально, мы учимся!

---


## 🚀 Как проверить работу

Когда напишешь код, запусти сервер:

1.  Открой файл `main.py`.
2.  Нажми кнопку **Play (▶)** в верхнем правом углу VS Code (или нажми F5).
3.  В терминале появится надпись: `Uvicorn running on http://127.0.0.1:8000`.
4.  Нажми клавишу **Ctrl** (или Cmd) и кликни мышкой по этой ссылке в терминале.
5.  В браузере откроется твой сайт.

Нажми кнопку **"Обновить данные"** на сайте. Если всё сделано верно, страница перезагрузится, и ты увидишь список цитат.

**Удачи! Да прибудет с тобой сила Python! 🐍**